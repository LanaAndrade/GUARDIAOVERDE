spring.application.name=guardiaoeverde

# segurança HTTP Basic (exige login em todos os endpoints)
spring.security.user.name=admin
spring.security.user.password=admin
spring.security.user.roles=USER

# Conexão Oracle (ou H2, se preferir testar localmente)
spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
spring.datasource.username=rm552596
spring.datasource.password=020503
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Limiar para disparar chamado/alerta automático
threshold.umidade=30.0
threshold.temperatura=40.0

# Período de checagem do MonitorService (em milissegundos)
monitor.periodo-ms=60000
